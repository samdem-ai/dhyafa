# Flat node_modules so React Native / Metro can resolve transitive deps
# (e.g. @babel/runtime, whatwg-fetch, event-target-shim). pnpm's default
# isolated linker hides these from Metro. Expo-recommended for monorepos.
node-linker=hoisted
