# deploy/.env — consumed by deploy/docker-compose.yml on the VPS.
# Fill SUPABASE_* from `supabase status` after the self-hosted stack is up.
# Never commit the real file.
PUBLIC_API_URL=http://51.210.43.80:8443
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
